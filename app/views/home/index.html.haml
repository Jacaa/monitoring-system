= render 'layouts/navbar'
= render 'layouts/flash'
.container
  .row
    #camera-view
  - if logged_in?
    .row
      %table#events
        %tr
          %th Number
          %th Human
          %th Walked in
          %th Date
          %th Hour
          %th Photo
          = render @events
